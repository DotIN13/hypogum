{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "brave-search": {
      "type": "local",
      "command": ["npx", "-y", "@brave/brave-search-mcp-server"],
      "enabled": true,
      "env": { "BRAVE_API_KEY": "{env:BRAVE_API_KEY}" }
    },
    "hypogum": { "enabled": false }
  }
}
